name: Build and Publish

on:
  push:
    branches: [ main ]
    tags:
      - '*'
  pull_request:
    branches: [ main ]

jobs:
  build:
    runs-on: ubuntu-latest
    steps:
    - uses: actions/checkout@v3
    - name: Set up JDK 17
      uses: actions/setup-java@v2
      with:
        java-version: '17'
        distribution: 'temurin'
        cache: maven
    - uses: olegtarasov/get-tag@v2.1.1
      if: startsWith(github.ref, 'refs/tags/')
      id: tagName
    - name: Set buildnumber
      if: startsWith(github.ref, 'refs/tags/')
      id: versionLabel
      run: echo "::set-output name=result::${{ steps.tagName.outputs.tag }}.$(date +'%Y%m%d').${{github.run_number}}.${{ github.run_id }}"
    - name: Set versions
      if: startsWith(github.ref, 'refs/tags/')
      env:
        ARTIFACT_VERSION: ${{ steps.buildnumber.outputs.result }}
      run: mvn versions:set -DnewVersion=${{ steps.versionLabel.outputs.result }}
    - name: Build with Maven
      run: mvn -B package
    - name: Cache build artifacts
      id: cache-maven-artifacts
      uses: actions/cache@v3
      with:
        path: target
        key: artifacts-${{github.run_number}}-${{github.run_attempt}}
    outputs:
      tagName: ${{ steps.tagName.outputs.tag }}
      versionLabel: ${{ steps.versionLabel.outputs.result }}

  publish:
    if: startsWith(github.ref, 'refs/tags/')
    needs: build
    runs-on: ubuntu-latest
    steps:
    - uses: actions/checkout@v3
    - name: Cache build artifacts
      id: cache-maven-artifacts
      uses: actions/cache@v3
      with:
        path: target
        key: artifacts-${{github.run_number}}-${{github.run_attempt}}
    - name: Check if cache restored artifacts
      run: |
        cd target
        ls -la
    - name: Log in to Docker Hub
      env:
        DOCKER_USERNAME: ${{ secrets.DOCKER_USERNAME }}
        DOCKER_PASSWORD: ${{ secrets.DOCKER_PASSWORD }}
      run: |
        docker login -u $DOCKER_USERNAME -p $DOCKER_PASSWORD
    - name: Deploy Docker image
      env:
        BUILDNUMBER: ${{needs.build.outputs.versionLabel}}
      run: |
        docker build . --build-arg JAR_FILE=target/flixtraktor-$BUILDNUMBER.jar \
          --tag ${{ secrets.DOCKER_USERNAME }}/flixtraktor:$BUILDNUMBER
        docker push ${{ secrets.DOCKER_USERNAME }}/flixtraktor:$BUILDNUMBER